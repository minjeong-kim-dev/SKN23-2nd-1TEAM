1. 환각 억제 점수 0.987은 어떤 방식으로 산출했나요?

- LangSmith의 LLM-as-a-Judge 방식으로 산출했습니다.
100문항 각각에 대해 LLM 평가자가 0~1 사이의 환각 점수를 매기고, 그 평균값이 0.987입니다.
평가 기준은 네 가지입니다. 매뉴얼 기반, 허위가 없는지, 지원 브랜드인지, 안전 경고를 포함했는지입니다.

2. 평가셋 100문항은 어떤 기준으로 만들었나요?

- 정답 유도 50문항 + 방어/함정 50문항으로 구성했습니다.
정답 유도는 매뉴얼에 실제 답이 있는 정상 질문으로, 챗봇이 정확하게 찾아내는지 검증합니다.
함정 50문항은 5가지 유형으로 나눴습니다.
미지원 브랜드 10건, 존재하지 않는 허위 에러코드 10건, 안전 위반 유도 10건, 브랜드 혼용 10건, 도메인 외 질문 10건입니다. 
정상 질문과 함정 질문을 50:50으로 구성해 균형 잡힌 평가를 목표로 했습니다.

3. 하이브리드 검색에서 Dense Retrieval과 BM25를 함께 쓴 이유는 무엇인가요?

- 벡터 검색(Dense Retrieval)은 의미적으로 유사한 내용을 잘 찾지만, 키워드 매칭에 약하고, 
BM25는 키워드 매칭에 강하지만, 유사한 내용을 찾는 것에 약하기 때문에 두 방식이 서로의 약점을 보완할 수 있다고 생각하여 같이 사용하였습니다.

4. Reranker는 어떤 문제를 해결하기 위해 적용했나요?

- BM25와 벡터 검색은 키워드 일치율·유사도로만 순위를 정하기 때문에, 실제로 답변에 유용한 문서가 상위에 오지 않을 수 있습니다. Reranker는 질문과 후보 문서를 함께 입력받아 실제 관련성을 재평가하고 순위를 조정해 이 문제를 해결합니다.

5. LangGraph에서 Supervisor와 Specialist Agent의 역할 차이는 무엇인가요?

- Supervisor는 분류만, Specialist는 답변 생성만 담당합니다.
Supervisor는 질문을 받아 로봇·용접·전기·일반 중 하나의 도메인으로 분류하고 해당 Specialist로 라우팅하는 역할만 합니다. 
Specialist는 Supervisor가 넘겨준 도메인에 맞는 RAG 검색을 실행하고 매뉴얼 기반 답변을 생성합니다.
역할을 분리한 이유는 하나의 프롬프트로 4개 도메인을 동시에 처리하면 전문성이 분산되고 환각이 증가할 수 있기 때문입니다.

6. 브랜드 배타성 규칙은 실제로 어떻게 구현했나요?

- 생성 전 규칙과 생성 후 검증의 2단계로 구현했습니다.
생성 전에서는 "지원하는 6대 브랜드 중 사용자가 언급한 단 1개 브랜드 매뉴얼만 근거로 답변하라"는 절대 규칙을 명시했습니다.
생성 후에는 환각 검증기의 브랜드 배타성 기준에서 "타 브랜드와 섞어 설명했는가"를 확인해 섞였으면 "no"를 반환합니다.
이중 구조로 방어/함정 50건 중 브랜드 관련 10건 전수 거부했습니다.

7. 본인이 프로젝트 01에서 직접 구현한 코드 범위는 어디까지인가요?

-> 수정 : 크게 두 영역을 담당했습니다.
첫번째, 시스템 프롬프트 설계에서 환각 방지 공통 원칙, Supervisor 프롬프트, 각 도메인(로봇·용접·전기·일반) 전문가 프롬프트, 환각 검증기 프롬프트를 작성했습니다.
두번째, Django 백엔드에서는 7개 테이블 ORM 모델 설계와 관리자 대시보드 API 4종(일일 요약·라인별 추이·최근 로그·에러코드 TOP3) 및 전체 로그 조회 API를 구현했습니다.

8. Django ORM 7개 테이블의 핵심 설계와 관계를 설명해주세요.

-> 수정 : 공장 현장의 흐름을 그대로 테이블로 모델링했습니다.
RobotModel(로봇 종류) → RobotDevice(개별 장비) → RobotErrorLog(에러 기록) → RobotErrorSession(상담 세션) 순으로 이어지는 1:N 구조이고, 세션 아래에 RobotErrorChatHistory(대화 내역), RobotErrorChecklistItem(점검 항목), EngineerCall(엔지니어 호출)이 연결됩니다.
삭제 정책은 상황에 따라 두 가지로 나눴습니다. 장비가 폐기되면 연관 로그도 의미가 없어지므로 함께 삭제되도록 CASCADE를 적용했고, 에러 로그가 삭제되더라도 상담 이력은 대시보드 통계에 계속 필요하기 때문에 SET_NULL로 연결만 끊고 세션은 보존했습니다.

9. select_related와 prefetch_related를 왜 사용했나요?

-> 수정 : N+1 문제를 해결하기 위해 사용했습니다.
에러 로그 100건을 조회할 때 각 로그의 장비 정보를 개별로 조회하면 쿼리가 101번 발생합니다. 이는 데이터가 많아질수록 응답이 느려지고 서버 부하가 커집니다. 이를 해결하기 위한 두가지 방법이 있습니다.
첫번째, select_related는 "내가 장비 번호를 알고 있을 때" 사용합니다. 에러 로그가 장비 번호를 직접 갖고 있으므로, SQL JOIN으로 로그와 장비 정보를 한 번에 가져옵니다.
두번째, prefetch_related는 "상대방이 나를 참조할 때" 사용합니다. 세션이 에러 로그 번호를 갖고 있는 구조라 JOIN을 쓰면 에러 로그 한 건이 세션 수만큼 중복 출력됩니다. 그래서 에러 로그 목록을 먼저 가져온 뒤, 세션을 WHERE IN으로 한 번에 가져옵니다.
결과적으로 101번 쿼리를 3번으로 줄였습니다.

10. Steam 프로젝트에서 이탈을 30일 기준으로 정의한 이유는 무엇인가요?

- 게임 업계에서 MAU(월간 활성 유저)를 30일 단위로 측정하는 관행을 참고했습니다.
30일이 "비활성 시작을 가장 빠르게 탐지할 수 있는 균형점"이라고 판단하여
(마지막 플레이 날짜 - 리뷰 작성 날짜) < 30일이면 이탈(1)로 정의했습니다.

11. 데이터 누수 방지를 위해 제외한 컬럼과 이유는 무엇인가요?

- 제외한 컬럼은 리뷰 후 경과일(days_after_review), 마지막 플레이 날짜(last_played), 리뷰 생성 시각(timestamp_created), 리뷰 수정 시각(timestamp_updated)입니다.
이 컬럼들은 이탈 타겟을 만드는 데 직접 사용된 값으로, 모델에 넣으면 정답을 미리 알려주는 것과 같습니다.
실제 서비스 상황에서는 리뷰 작성 시점에만 데이터를 알 수 있고, 마지막 플레이 날짜는 미래 정보이므로 학습에 사용하면 안 됩니다.
이를 포함하면 학습 정확도는 높아지지만 실제 예측에서는 쓸 수 없는 모델이 됩니다.

12. LogisticRegression과 LightGBM 중 어떤 모델을 최종 선택했고, 왜 그렇게 판단했나요?

- 이 프로젝트의 목표가 이탈 유저를 최대한 놓치지 않는 것이므로 Recall을 우선 지표로 삼았고, 그 기준에서는 LogisticRegression을 선택했습니다.
Recall이 가장 높은 LogisticRegression을(0.85)을 선택했으며, 이탈 유저를 놓치는 비용이 오탐 비용보다 크다고 판단했기 때문입니다.
F1 균형이 더 중요하다면 LightGBM(0.83)이 적합합니다.

13. Recall을 우선 지표로 설정한 이유는 무엇인가요?

- 이탈 예측에서 발생할 수 있는 두 가지 오류의 비용이 다르기 때문입니다.
이탈자를 놓치면(FN) 리텐션 대응 기회 자체를 잃습니다.
정상 유저를 이탈로 오판하면(FP) 불필요한 프로모션 비용이 발생하지만 회복 가능합니다.
이탈자를 놓치는 비용이 더 크기 때문에 Recall을 우선 지표로 설정했습니다.

14. Threshold를 조정했다면 어떤 기준으로 결정했나요?

- 이 프로젝트에서는 기본값인 0.5를 사용했습니다.
Threshold는 모델이 출력한 이탈 확률을 0/1로 변환하는 기준값입니다.
낮출수록 더 많은 유저를 이탈로 분류해 Recall이 올라가지만 Precision이 내려갑니다.
0.5에서 LR의 Recall이 이미 0.85로 높았기 때문에 추가 조정 없이 사용했습니다.
만약 Recall을 더 높여야 한다면 threshold를 0.3~0.4로 낮추고 Precision-Recall 곡선으로 최적값을 탐색하는 방법을 사용할 수 있습니다.

15. 포트폴리오의 기술 중 실제로 가장 자신 있게 코드 수준에서 설명할 수 있는 것은 무엇인가요?

- 파생 변수 no_recent_play 설계입니다.

코드는 한 줄입니다.
df["no_recent_play"] = (df["playtime_last_two_weeks"] == 0).astype(int)

동작 설명은 세 단계입니다.
첫번째, df["playtime_last_two_weeks"] == 0 은 최근 2주 플레이 시간이 0인 행을 True/False로 반환합니다.
두번째, .astype(int) 는 True → 1, False → 0 으로 변환합니다.
세번째, 결과적으로 최근 2주간 한 번도 플레이하지 않은 유저는 1, 플레이한 유저는 0이 됩니다.

이탈 가설은 "2주간 플레이가 완전히 단절된 유저는 이탈 강한 신호"입니다.
상관분석에서 churn과의 상관계수가 0.26으로 확인됐고, 이 가설이 데이터로 검증됐기 때문에 최종 8개 피처에 포함했습니다.
