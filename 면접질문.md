3. 프로젝트 02: Steam 상위 50위 게임 장르별 이탈 예측 모델 — 면접 예상 답변

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  
3-1. 문제 정의·데이터 검증  
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  

1. Steam 상위 50위 게임을 분석 대상으로 선택한 이유는 무엇인가요?
```text
두 가지 이유입니다. 첫째, 상위 50위 게임은 리뷰 수가 충분해서 통계적으로 의미 있는 분석이 가능합니다. 리뷰 수가 적은 게임은 이탈 신호를 제대로 잡기 어렵습니다. 둘째, 인기 게임일수록 유저 행동 데이터가 다양하고 장르도 여러 가지여서, 장르별 이탈 패턴 차이를 비교 분석할 수 있었습니다. 반대로 너무 범위를 넓히면 데이터 품질 편차가 커지는 문제도 있었습니다.
```

2. 사용한 데이터는 어디에서 수집했고, 데이터 규모는 어느 정도였나요?
```text
수집은 팀원이 담당했고, Steam 공개 리뷰 API를 통해 수집한 것으로 알고 있습니다. 데이터 규모는 약 103만 건(1,030,656건)이었고, 원본 컬럼은 28개였습니다. 수집 기간은 2025년 1월부터 2026년 1월까지 1년 치 데이터입니다.
```

3. 사용자 단위 데이터인지, 리뷰 단위 데이터인지, 게임 단위 데이터인지 설명해주세요.
```text
리뷰 단위 데이터입니다. 한 행이 한 건의 리뷰를 의미하고, 해당 리뷰를 작성한 유저의 플레이타임, 보유 게임 수, 마지막 플레이 시점, 리뷰 내용 등이 컬럼으로 포함되어 있습니다. 한 유저가 여러 게임에 리뷰를 남겼다면 여러 행으로 존재합니다.
```

4. "리뷰 시점 − 마지막 플레이 ≥ 30일"을 이탈 기준으로 정한 이유는 무엇인가요?
```text
리뷰 작성 후 마지막 플레이까지 30일 미만이면 이탈로 정의했습니다. 즉, 리뷰를 쓴 뒤 30일 이내에 플레이가 끊긴 유저를 이탈자로 분류한 것입니다. 코드로는 (마지막 플레이 날짜 − 리뷰 작성 날짜) < 30일이면 이탈(1)입니다. 30일 기준은 MAU(월간 활성 유저) 개념에서 따왔습니다. 30일 이상 활동이 없으면 비활성 유저로 분류하는 것이 업계에서 일반적으로 사용하는 기준입니다.
```

5. 14일, 30일, 60일 기준을 비교해봤나요?
```text
직접 세 기준으로 비교 실험을 하지는 않았지만, 각각의 문제를 판단해서 30일을 선택했습니다. 14일 기준은 너무 짧아서 단순히 바쁜 유저도 이탈로 오판할 수 있습니다. 60일 기준은 실제로 게임을 떠난 유저를 너무 늦게 포착해서 마케팅 액션을 취하기 어렵게 됩니다. 30일이 과잉 탐지와 지연 탐지 사이의 균형점이라고 판단했습니다.
```

6. 장르마다 플레이 주기가 다른데, 모든 장르에 동일하게 30일 기준을 적용한 한계는 없나요?
```text
있습니다. RPG나 스토리 중심 게임은 클리어 후 한동안 안 하다가 DLC가 나오면 다시 플레이하는 패턴이 있습니다. 이런 장르에서는 30일이 지나도 이탈이 아닐 수 있습니다. 반면 FPS처럼 매일 접속하는 장르에서는 30일이 너무 관대한 기준일 수 있습니다. 이 한계를 인식해서 장르를 '엔딩형 장르 여부(is_ending_genre)' 피처로 변환해서 모델에 반영했습니다.
```

7. 이탈 라벨이 없는 데이터에서 타겟을 직접 정의할 때 가장 주의해야 할 점은 무엇인가요?
```text
가장 중요한 것은 데이터 누수(Data Leakage)를 막는 것입니다. 타겟을 정의할 때 사용한 컬럼이 그대로 모델 입력으로 들어가면, 모델이 사실상 정답을 미리 보고 학습하는 것과 같습니다. 이 경우 성능이 비정상적으로 높게 나오지만 실제 운영에서는 전혀 쓸 수 없습니다. 그래서 타겟 정의에 사용한 컬럼을 반드시 제외해야 합니다.
```

8. 데이터 누수란 무엇이며, 이 프로젝트에서는 어떤 컬럼을 제외했나요?
```text
데이터 누수는 모델 학습 시 실제 운영 환경에서는 알 수 없는 미래 정보나 타겟 정보가 입력으로 포함되어 모델 성능이 가짜로 높아지는 현상입니다. 이 프로젝트에서는 리뷰 후 경과일(days_after_review), 마지막 플레이 날짜(last_played), 리뷰 생성 시각(timestamp_created), 리뷰 수정 시각(timestamp_updated)을 제외했습니다. 이 컬럼들은 이탈 타겟을 직접 계산하는 데 사용된 값이기 때문에, 그대로 넣으면 모델이 정답을 미리 아는 것과 같습니다.
```

9. playtime_forever나 last_played를 제외한 이유를 구체적으로 설명해주세요.
```text
last_played는 이탈 타겟을 직접 계산하는 데 사용한 컬럼이기 때문에 제외했습니다. 이 값이 모델에 들어가면 모델이 정답을 미리 보고 학습하는 데이터 누수가 발생합니다. playtime_forever(누적 플레이타임)는 이탈 정의에 직접 사용되지 않았기 때문에 최종 모델 입력에 포함했습니다. 제외된 것은 last_played와 타임스탬프 컬럼들입니다.
```

10. Train/Test 분리는 어떤 방식으로 했나요?
```text
이탈 비율을 유지하는 stratify 옵션을 사용해서 80:20으로 분리했습니다. 이탈률이 약 59.4%였기 때문에, 분리 후에도 Train과 Test 세트에서 같은 비율이 유지되도록 했습니다.
```

11. 시간 기준 분할을 적용하지 않았다면, 향후 데이터에서 발생할 수 있는 문제는 무엇인가요?
```text
시간 기준 분할을 하지 않으면 미래 데이터의 패턴이 Train에 포함될 수 있습니다. 예를 들어 2026년 1월 데이터가 Train에 있고 2025년 6월 데이터가 Test에 있으면, 모델이 미래를 보고 학습하는 셈이 됩니다. 실제 운영에서는 과거 데이터로 학습하고 미래를 예측해야 하기 때문에, 날짜 기준으로 분리하는 것이 더 현실적인 성능 평가 방법입니다. 이 부분은 이번 프로젝트의 개선 여지로 인식하고 있습니다.
```

12. 이탈 클래스와 잔존 클래스의 비율은 어느 정도였나요?
```text
이탈(1) 약 59.4%, 잔존(0) 약 40.6%였습니다. 이탈이 더 많은 불균형 데이터였습니다. RF와 LightGBM에는 class_weight='balanced'를 적용해서 소수 클래스(잔존)에 더 높은 가중치를 부여했습니다. LogisticRegression은 별도 가중치 없이 학습했습니다.
```


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  
3-2. Feature Engineering 질문  
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  

13. play_intensity는 어떻게 계산했나요?
```text
리뷰 시점까지의 플레이타임을 보유 게임 수로 나눈 값입니다. 계산식은 '리뷰시점 플레이타임 ÷ (보유 게임 수 + 1)'입니다. 게임을 많이 가지고 있는 유저일수록 특정 게임에 쏟는 집중도가 낮을 수 있어서, 이 비율이 낮을수록 해당 게임에 대한 몰입이 약하고 이탈 가능성이 높다는 가설로 만든 변수입니다. (+1은 0으로 나누는 오류를 막기 위해 추가했습니다.)
```

14. recent_ratio는 어떤 의미이며, 왜 이탈 예측에 유효하다고 생각했나요?
```text
최근 2주 플레이타임을 누적 플레이타임으로 나눈 값입니다. 계산식은 '최근 2주 플레이 ÷ (누적 플레이 + 1)'입니다. 전체 플레이 중 최근 활동이 차지하는 비중을 나타냅니다. 이 비율이 낮을수록 예전에는 많이 했지만 요즘 하지 않는다는 뜻이므로, 이탈이 진행 중인 신호로 볼 수 있습니다. 상관분석에서 이탈과 0.19의 상관관계를 확인했습니다.
```

15. no_recent_play는 어떤 기준으로 정의했나요?
```text
최근 2주 플레이타임이 0인 경우를 1로 표시한 이진 변수입니다. 코드는 한 줄로, df["no_recent_play"] = (df["playtime_last_two_weeks"] == 0).astype(int)입니다. 2주 동안 한 번도 플레이하지 않았다면 이탈의 강한 신호로 볼 수 있습니다. 상관분석에서 이탈과 0.26의 상관관계를 확인했습니다.
```

16. 리뷰 감성 점수는 어떻게 산출했나요?
```text
Steam 데이터에는 voted_up(추천 여부)이 이진 컬럼으로 제공됩니다. 별도의 자연어 감성 분석을 수행하지는 않았고, voted_up을 감성 방향의 대리 변수로 활용했습니다. 또한 weighted_vote_score(Steam이 계산한 가중 투표 점수)와 votes_up(도움이 됐다는 투표 수)도 리뷰 메타 변수로 포함했습니다.
```

17. 장르별 특성을 반영한 Feature는 실제로 어떤 것이 있었나요?
```text
장르 컬럼을 그대로 사용하는 대신, '엔딩형 장르 여부(is_ending_genre)'라는 이진 변수로 변환했습니다. 스토리 중심 RPG처럼 클리어 후 자연스럽게 플레이가 줄어드는 장르는 1로, FPS나 MOBA처럼 지속적으로 플레이하는 장르는 0으로 구분했습니다. 같은 30일 공백이라도 장르에 따라 이탈 의미가 달라지기 때문에 이 변수를 추가했습니다.
```

18. 최종 모델에 사용한 Feature 수는 몇 개였나요?
```text
최종 모델 입력에는 8개 피처를 사용했습니다. 플레이타임 3개(최근 2주, 리뷰 시점, 누적), 보유 게임 수 1개, 파생 변수 4개(no_recent_play, recent_ratio, play_drop_ratio, play_intensity)입니다.
```

19. Feature selection은 어떤 방식으로 진행했나요?
```text
상관분석을 통해 이탈 타겟과의 상관관계를 확인하고, 관련성이 높은 변수 위주로 선택했습니다. 즉시 이탈(quit_immediately) 0.55, 리뷰 몰입도(review_commitment) 0.36, 최근 단절(no_recent_play) 0.26 순으로 관련성을 확인했습니다. 데이터 누수가 있는 컬럼(last_played, timestamp 등)과 텍스트, ID 등 불필요 컬럼은 사전에 제거했습니다.
```

20. 상관분석만으로 Feature를 선택했을 때 발생할 수 있는 한계는 무엇인가요?
```text
두 가지 한계가 있습니다. 첫째, 상관분석은 선형 관계만 측정합니다. 변수와 이탈 간에 비선형 관계가 있어도 상관계수가 낮게 나와서 중요한 변수를 버릴 수 있습니다. 둘째, 서로 높은 상관관계를 가진 두 변수가 둘 다 선택되면 다중공선성 문제가 생길 수 있습니다. 이를 보완하려면 Feature importance나 SHAP 값 같은 모델 기반 방법을 추가로 활용하는 것이 좋습니다.
```

21. Feature importance를 확인했나요? 가장 영향력이 높았던 변수는 무엇이었나요?
```text
RandomForest나 LightGBM 모델을 통해 Feature importance를 확인할 수 있었습니다. 상관분석 결과와 유사하게 no_recent_play(최근 2주 미접속)와 recent_ratio(최근 활동성)가 가장 높은 영향력을 보였습니다. 최근 플레이 활동이 이탈을 예측하는 데 가장 직접적인 신호임을 확인할 수 있었습니다.
```

22. 사용자 행동 변수와 이탈 간의 관계에서 예상과 달랐던 결과가 있었나요?
```text
누적 플레이타임이 많은 유저도 이탈로 분류되는 경우가 있었습니다. 처음에는 오래 플레이한 유저가 더 충성도가 높을 것이라고 생각했는데, 과거에 많이 했더라도 최근 활동이 없으면 이탈로 잡히는 케이스가 많았습니다. 즉 누적 플레이타임보다 최근 활동성이 훨씬 강한 이탈 신호라는 것을 데이터로 확인했습니다.
```


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  
3-3. 모델링·평가 질문  
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  

23. RandomForest, LightGBM, LogisticRegression을 후보로 선정한 이유는 무엇인가요?
```text
세 모델이 서로 다른 특성을 가지고 있어서 비교 실험에 적합했기 때문입니다. LogisticRegression은 단순하고 해석이 쉬워서 기준선(baseline) 역할을 합니다. RandomForest는 여러 결정 트리를 앙상블해서 과적합에 강하고 Feature importance를 확인하기 좋습니다. LightGBM은 부스팅 계열로 일반적으로 성능이 높고 속도가 빠릅니다. 복잡성이 다른 세 모델을 비교해서 이 데이터에 가장 적합한 모델을 선택했습니다.
```

24. LogisticRegression이 Recall 0.85로 가장 높았는데, 최종 모델로 선택했나요?
```text
네, Recall을 핵심 지표로 설정했기 때문에 LogisticRegression을 최종 모델로 선택했습니다. Recall 0.85로 세 모델 중 가장 높았고, F1도 0.82로 LightGBM(0.83)과 크게 차이 나지 않았습니다. 이탈 유저를 최대한 빠짐없이 잡는 것이 목표였기 때문에 Recall을 기준으로 판단했습니다.
```

25. LightGBM의 F1-score가 더 높았는데, 운영 모델로 어떤 모델을 선택했고 그 이유는 무엇인가요?
```text
LogisticRegression을 선택했습니다. LightGBM이 F1은 0.83으로 약간 높지만, Recall이 0.81로 LogisticRegression(0.85)보다 낮습니다. 이 프로젝트의 목표는 이탈 유저를 최대한 놓치지 않는 것이기 때문에 Recall이 더 중요한 지표였습니다. 또한 LogisticRegression은 해석이 쉬워서 마케팅 담당자에게 "이 유저가 왜 이탈 위험군으로 분류됐는지"를 설명하기도 편합니다.
```

26. Recall을 핵심 지표로 설정한 이유는 무엇인가요?
```text
이탈 예측의 목적이 위험 유저를 사전에 발견해서 마케팅 액션을 취하는 것이기 때문입니다. 이탈 유저를 놓치는 것(FN, False Negative)이 비용이 더 큽니다. 이탈할 유저에게 마케팅을 못 하면 유저를 잃게 됩니다. 반면 이탈하지 않을 유저에게 불필요하게 마케팅을 하는 것(FP)은 비용이 들지만 유저를 잃지는 않습니다. 따라서 FN을 줄이는 Recall을 우선 지표로 설정했습니다.
```

27. Precision이 너무 낮아지면 어떤 비즈니스 문제가 생길 수 있나요?
```text
실제로 이탈하지 않을 유저에게 과도하게 마케팅을 하게 됩니다. 예를 들어 쿠폰이나 혜택을 뿌리는 캠페인이라면 불필요한 비용이 발생합니다. 또한 이탈 위험이 없는 유저에게 계속 푸시 알림을 보내면 오히려 불쾌감을 줘서 진짜 이탈을 유발할 수도 있습니다. Precision이 너무 낮아지면 마케팅 ROI가 떨어집니다.
```

28. Recall과 Precision 사이의 trade-off를 어떻게 설명할 수 있나요?
```text
이탈 예측에서 더 많은 유저를 이탈로 잡으려 하면(Recall 상승) 실제로 이탈하지 않는 유저도 같이 잡히게 됩니다(Precision 하락). 반대로 확실한 이탈 유저만 잡으려 하면(Precision 상승) 이탈 유저를 일부 놓치게 됩니다(Recall 하락). 어느 것을 더 중요하게 볼지는 비즈니스 상황에 따라 다릅니다. 이 프로젝트에서는 이탈 유저를 최대한 놓치지 않는 것을 목표로 했기 때문에 Recall을 우선했습니다.
```

29. 클래스 불균형 문제는 어떤 방식으로 해결했나요?
```text
RF와 LightGBM에 class_weight='balanced' 옵션을 적용했습니다. 이탈(59.4%)이 잔존(40.6%)보다 많아서 완전한 불균형은 아니지만, 모델이 다수 클래스에 편향되는 것을 막기 위해 적용했습니다. LogisticRegression은 별도 가중치 없이 학습해서 세 모델의 접근 방식을 비교했습니다.
```

30. class_weight='balanced'는 내부적으로 어떤 역할을 하나요?
```text
각 클래스의 샘플 수에 반비례하는 가중치를 자동으로 계산해서 손실 함수에 반영합니다. 샘플이 적은 클래스의 오분류에 더 큰 패널티를 부여해서, 모델이 소수 클래스도 잘 학습하도록 유도합니다. 예를 들어 잔존 클래스(40.6%)에 더 높은 가중치를 부여해서, 잔존 유저를 이탈로 잘못 분류하는 것에 더 큰 비용을 매깁니다.
```

31. SMOTE 같은 오버샘플링 방법은 검토했나요? 사용하지 않았다면 이유는 무엇인가요?
```text
검토하지 않았습니다. 이 데이터는 이탈 59.4%, 잔존 40.6%로 극단적인 불균형은 아니었습니다. class_weight='balanced'로 충분히 대응할 수 있었고, SMOTE는 소수 클래스 데이터를 인위적으로 생성하기 때문에 실제 분포와 달라질 수 있다는 우려도 있었습니다. 향후 불균형이 더 심한 데이터라면 SMOTE를 적용해볼 여지가 있습니다.
```

32. Threshold는 0.5 그대로 사용했나요, 조정했나요?
```text
포트폴리오에서는 기본 0.5를 사용했습니다. 다만 Recall을 더 높이려면 Threshold를 낮추는 방향으로 조정하는 것을 고려할 수 있습니다. 예를 들어 Threshold를 0.3으로 낮추면 더 많은 유저를 이탈 위험군으로 분류해서 Recall이 올라가지만 Precision이 낮아집니다.
```

33. Threshold를 조정했다면 어떤 기준으로 최적값을 정했나요?
```text
비즈니스 목표에 따라 다르게 설정합니다. 이 프로젝트에서는 Recall을 최우선으로 했기 때문에, Precision이 일정 수준(예: 0.6 이상)을 유지하면서 Recall이 최대화되는 지점을 Precision-Recall Curve에서 찾는 방식으로 최적 Threshold를 정할 수 있습니다. 실제 운영이라면 마케팅 비용 대비 효과를 고려해서 비즈니스 담당자와 함께 결정하는 것이 좋습니다.
```

34. Confusion Matrix에서 FN과 FP는 각각 어떤 의미인가요?
```text
FN(False Negative)은 실제로 이탈할 유저인데 모델이 잔존으로 예측한 경우입니다. 이탈 유저를 놓쳐서 마케팅 기회를 잃는 것입니다. FP(False Positive)는 실제로 잔존할 유저인데 모델이 이탈로 예측한 경우입니다. 이탈하지 않을 유저에게 불필요한 마케팅을 하는 것입니다.
```

35. 이탈 예측 업무에서 FN을 더 중요하게 본 이유는 무엇인가요?
```text
FN이 발생하면 이탈할 유저를 사전에 놓쳐서 유저를 잃게 됩니다. FP가 발생하면 이탈하지 않을 유저에게 불필요한 마케팅 비용이 들지만 유저는 잃지 않습니다. 유저 이탈은 한 번 발생하면 다시 돌아오기 어렵고, 신규 유저 유치 비용이 훨씬 크기 때문에 FN의 비용이 FP보다 크다고 판단했습니다.
```

36. 교차검증을 수행했나요? 수행하지 않았다면 결과 신뢰도에 어떤 한계가 있나요?
```text
이번 프로젝트에서는 단순 Train/Test 분리만 수행했고 교차검증은 하지 않았습니다. 교차검증 없이 단일 분리로만 평가하면, 데이터 분리 방식에 따라 성능이 달라질 수 있습니다. 특정 분리 결과가 운이 좋았거나 나빴을 수 있어서 성능의 신뢰 구간을 알기 어렵습니다. k-fold 교차검증을 추가하면 더 안정적인 성능 추정이 가능합니다.
```

37. AUC-ROC나 PR-AUC를 사용하지 않은 이유는 무엇인가요?
```text
직접 사용하지는 않았지만, 사용했다면 더 좋은 평가가 됐을 것입니다. Recall과 F1에 집중해서 비교 실험을 진행했는데, PR-AUC는 클래스 불균형 상황에서 특히 유용한 지표입니다. 향후 모델 비교 시 PR-AUC를 추가하면 Threshold에 독립적인 전체적인 성능을 더 잘 볼 수 있습니다.
```

38. 모델 성능이 높더라도 실제 운영에서 성능이 떨어질 수 있는 이유는 무엇인가요?
```text
두 가지가 주요 원인입니다. 첫째, 시간에 따른 데이터 분포 변화(Concept Drift)입니다. 게임 업데이트나 시즌 이벤트에 따라 유저 행동 패턴이 달라지면, 과거 데이터로 학습한 모델이 현재를 잘 예측하지 못합니다. 둘째, 학습 데이터와 실제 운영 데이터 간의 환경 차이입니다. 이 프로젝트 데이터는 특정 기간의 리뷰 데이터이기 때문에, 새로운 기간이나 다른 게임에서는 성능이 다를 수 있습니다.
```

39. 장르별로 모델 성능 차이가 있었나요?
```text
장르별로 별도 성능 분석을 수행하지는 않았습니다. 다만 is_ending_genre 변수로 장르 특성을 반영했기 때문에, 엔딩형 장르와 지속형 장르에서 모델 반응이 다를 것으로 예상했습니다. 실제 운영이라면 장르별로 별도 모델을 학습하거나, 장르를 조건으로 서브그룹 분석을 하는 것이 더 정밀한 예측을 가능하게 합니다.
```

40. 모델 성능 외에 설명 가능성을 고려했다면 어떤 방법을 사용할 수 있나요?
```text
세 가지 방법을 활용할 수 있습니다. 첫째, LogisticRegression의 경우 각 Feature의 계수(coefficient)를 직접 해석할 수 있어서 어떤 변수가 이탈에 얼마나 영향을 미치는지 설명하기 쉽습니다. 둘째, SHAP(SHapley Additive exPlanations)을 사용하면 각 예측에서 Feature별 기여도를 시각적으로 보여줄 수 있습니다. 셋째, Feature importance 시각화로 마케팅 담당자에게 "어떤 행동 패턴이 이탈과 관련 있는지" 쉽게 설명할 수 있습니다.
```


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   
3-4. 서비스화·활용 관점 질문  
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  

41. Streamlit 대시보드에서 사용자가 가장 먼저 확인해야 하는 정보는 무엇인가요?
```text
이탈 위험군 유저 수와 장르별 이탈 위험 분포입니다. 마케팅 담당자 입장에서 오늘 당장 액션을 취해야 할 유저가 몇 명인지, 어느 장르에서 특히 이탈이 많은지를 한눈에 파악하는 것이 가장 중요합니다. 이를 기반으로 어떤 캠페인을 어떤 타겟에게 집중할지 결정할 수 있습니다.
```

42. 이탈 위험군을 어떤 기준으로 구분했나요?
```text
모델이 출력하는 이탈 확률(0~1)을 기반으로 구분했습니다. Recall 중심으로 설계했기 때문에 확률이 일정 수준 이상이면 위험군으로 분류했습니다. 실제 서비스라면 고위험(0.8 이상), 중위험(0.5~0.8), 저위험(0.5 미만) 같은 구간을 설정해서 위험도에 따라 다른 마케팅 액션을 연결할 수 있습니다.
```

43. 위험군 사용자에게 어떤 마케팅 액션을 제안할 수 있나요?
```text
위험도 수준에 따라 다르게 접근하면 좋습니다. 고위험군에는 게임 내 아이템 지급이나 복귀 보상 같은 강한 인센티브를 제공합니다. 중위험군에는 새 업데이트 소식 푸시 알림이나 이벤트 안내를 통해 관심을 다시 끌 수 있습니다. 장르별로도 다르게 접근할 수 있는데, 엔딩형 게임이라면 DLC나 새 콘텐츠 알림이 효과적입니다.
```

44. ROI 시뮬레이션은 어떤 입력값과 계산 로직으로 구성했나요?
```text
ROI 시뮬레이션은 마케팅 담당자가 직접 입력한 비용과 모델 예측 결과를 연결하는 구조입니다. 입력값으로는 유저당 마케팅 비용, 캠페인 전환율 가정, 유저 1인당 예상 매출을 사용합니다. 계산 로직은 "모델이 잡은 이탈 위험군 수 × 전환율 × 유저당 매출 − 위험군 수 × 유저당 마케팅 비용"으로 기대 ROI를 계산합니다. 다만 이번 프로젝트에서는 실제 캠페인 데이터가 없어서 가정 기반의 시뮬레이션이었습니다.
```

45. 마케팅 담당자가 이 모델을 신뢰하지 않는다면 어떤 지표와 화면을 제공해야 하나요?
```text
세 가지를 보여주면 좋습니다. 첫째, Confusion Matrix를 시각화해서 "이 모델이 100명 중 몇 명을 맞게 잡는지"를 직관적으로 보여줍니다. 둘째, 이탈로 분류된 유저의 실제 행동 데이터(최근 플레이 없음, 리뷰 후 접속 안 함 등)를 나란히 보여줘서 "왜 이 유저가 이탈 위험군인지" 납득할 수 있게 합니다. 셋째, 소규모 A/B 테스트 결과를 제시해서 실제 캠페인 성과와 모델 예측 일치율을 증거로 보여줍니다.
```

46. 예측 결과가 실제 캠페인 성과로 이어졌는지 검증하려면 어떤 실험이 필요하나요?
```text
A/B 테스트가 필요합니다. 이탈 위험군으로 예측된 유저를 두 그룹으로 나눠서, 한 그룹에는 마케팅 캠페인을 적용하고 다른 그룹에는 적용하지 않습니다. 일정 기간 후 두 그룹의 실제 이탈률을 비교하면 모델이 올바른 유저를 잡았는지, 그리고 캠페인이 효과가 있는지 동시에 검증할 수 있습니다.
```

47. 실제 서비스 데이터가 매일 들어온다면 모델 재학습 주기는 어떻게 정하겠습니까?
```text
두 가지 기준으로 정하겠습니다. 첫째, 주기 기반으로 월 1회나 분기 1회 정기 재학습을 설정합니다. 게임 업데이트나 시즌 이벤트 직후에는 유저 행동이 바뀌므로 그 시점에 맞춰 추가로 재학습하는 것도 좋습니다. 둘째, 성능 기반으로 운영 중 모델 성능이 일정 기준 이하로 떨어지면 자동으로 재학습을 트리거하는 구조를 만듭니다.
```

48. 데이터 분포가 바뀌는 concept drift는 어떻게 감지하겠습니까?
```text
두 가지 방법을 사용하겠습니다. 첫째, 모니터링입니다. 모델이 예측한 이탈률과 실제 이탈률 간의 차이를 주기적으로 추적합니다. 차이가 점점 커지면 드리프트 신호입니다. 둘째, Feature 분포 모니터링입니다. 학습 데이터와 최근 데이터 간에 주요 Feature(예: recent_ratio, no_recent_play)의 분포를 비교해서 유의미한 변화가 있으면 경보를 발생시킵니다.
```

49. 모델이 특정 장르 또는 특정 사용자군에서 편향될 위험은 없나요?
```text
있습니다. 학습 데이터에서 특정 장르나 특정 플레이 타임 구간의 유저가 많으면, 모델이 그 패턴에 과도하게 최적화될 수 있습니다. 예를 들어 FPS 유저가 데이터의 대부분이라면 RPG 유저의 이탈 패턴은 잘 못 잡을 수 있습니다. 이를 줄이려면 장르별 성능을 서브그룹으로 분석하고, 특정 장르에서 성능이 낮으면 해당 장르 데이터를 보강하거나 별도 모델을 구축하는 방향을 고려해야 합니다.
```

50. 이 프로젝트를 실제 게임사 환경에 적용할 때 가장 먼저 보완할 기능은 무엇인가요?
```text
두 가지입니다. 첫째, 실시간 예측 파이프라인 구축입니다. 현재는 배치 처리 방식이지만, 실제 게임사에서는 유저 행동이 발생할 때마다 이탈 위험도를 업데이트하는 실시간 구조가 필요합니다. 둘째, 게임사 내부 데이터 연동입니다. 접속 로그, 구매 이력, 게임 내 활동 데이터 등 훨씬 다양하고 정밀한 데이터를 활용하면 이탈 예측 정확도를 크게 높일 수 있습니다. Steam 공개 데이터만으로는 실제 게임 내 행동을 충분히 반영하기 어렵습니다.
```
