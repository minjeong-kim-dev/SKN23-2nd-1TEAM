## Unix 타임스탬프 → datetime 변환

Steam API는 날짜를 **숫자**로 저장함 (1704067200 같은 형태)
이걸 Unix 타임스탬프라고 하며 "1970년 1월 1일부터 몇 초가 지났는가"를 의미

```python
pd.to_datetime(df["timestamp_created"], unit="s")
# unit="s" → 이 숫자의 단위가 초(seconds)라고 알려주는 것
# 1704067200 → 2024-01-01 00:00:00 으로 변환

# 변환하는 이유: 날짜끼리 빼서 '며칠 차이'를 계산하려면 datetime 타입이어야 함
# 숫자끼리 빼면 "초 차이"만 나옴
```

---

## df.loc[조건, 컬럼] = 값

**조건에 해당하는 행의 특정 컬럼을 값으로 바꿔라**

```python
df.loc[df["last_played"] == 0, "churn"] = 1
# df에서 last_played가 0인 행을 찾아서
# 그 행의 churn 컬럼을 1로 바꿔라

# last_played == 0 의미: 마지막 플레이 기록이 없음 → 이탈로 간주
```

---

## value_counts() — 클래스 분포 확인

```python
df['churn'].value_counts()
# churn
# 1    122461   ← 이탈 유저 수 (59%)
# 0     83671   ← 유지 유저 수 (41%)
```

**왜 확인하나?**  
이탈(1)과 유지(0) 비율이 심하게 차이나면 클래스 불균형 문제 발생  
예: 이탈 90%, 유지 10%이면 모델이 전부 이탈로 찍어도 정확도 90%  
→ 이 프로젝트에서 `class_weight='balanced'` 쓴 근거

---

## isnull().sum() — 결측치 확인

```python
df.isnull().sum()
# 컬럼별로 빠진 값이 몇 개인지 출력
# 숫자가 크면 결측치 많음 → 삭제 or 대체 결정 필요
```

**결측치 처리 방법**
- 결측치가 너무 많은 컬럼 → 삭제
- 수치형 컬럼 결측치 → 중앙값으로 대체
  - **평균 대신 중앙값 쓰는 이유: 플레이타임처럼 극단값이 많으면 평균이 왜곡됨**

---

## apply() + lambda

```python
df['genre'].apply(lambda x: ...)
```

**apply()**: 컬럼의 값을 한 행씩 꺼내서 함수에 넣고, 결과를 새 컬럼에 저장  
**lambda x**: 이름 없는 함수, x는 apply가 한 행씩 자동으로 넣어주는 값

```python
# x는 내가 정한 이름이 아님, 아무 이름이나 써도 됨
lambda x: ...
lambda value: ...   # 다 같은 코드

# for문으로 풀어쓰면:
result = []
for x in df['genre']:
    result.append(함수(x))
```

---

## 리스트 컴프리헨션 — 안전한 컬럼 삭제

```python
id_cols = ['recommendationid', 'steamid', 'appid_1', 'rnd', 'language']
id_cols = [c for c in id_cols if c in df.columns]
df = df.drop(columns=id_cols)
```

**왜 필요한가?**  
리스트에 있는 컬럼이 df에 없으면 drop 시 KeyError 오류 발생  
→ 실제로 df에 있는 것만 걸러낸 후 삭제 (안전장치)  

```python
# for문으로 풀어쓰면:
result = []
for c in id_cols:
    if c in df.columns:
        result.append(c)
id_cols = result
```

---

## describe() 해석법

| 행 | 의미 |
|----|------|
| count | 결측치 없는 값 개수 |
| mean | 평균 (극단값에 영향 받음) |
| std | 표준편차 (클수록 값이 퍼져있음) |
| min / max | 최솟값 / 최댓값 |
| 50% | 중앙값 |

> **핵심: mean과 50%(중앙값)의 차이가 크면 → 로그 변환 필요 신호**

```
playtime_forever
- mean = 1.421302e+04  →  14,213분
- 50%  = 4.408000e+03  →   4,408분

mean이 중앙값의 3배 → 소수의 헤비유저가 평균을 끌어올린 것 → 오른쪽으로 치우친 분포 → 로그 변환 필요
```

**e+04 읽는 법**
```
1.421302e+04 = 1.421302 × 10⁴ = 14,213   (소수점 오른쪽으로 4칸)
4.408000e+03 = 4.408000 × 10³ =  4,408   (소수점 오른쪽으로 3칸)
```

---

## 왜도(Skewness) — 치우침 수치 확인

```python
df[log_cols].skew()
```

| 절댓값 범위 | 의미 |
|------------|------|
| 1 미만 | 비교적 균등 → 로그 변환 불필요 |
| 1 ~ 3 | 약간 치우침 |
| **3 이상** | **심하게 치우침 → 로그 변환 필요** |

---

## ⭐ 로그 변환이란?

> **한쪽으로 몰린 분포를 펼쳐주는 것**
> 모델이 극단값(헤비유저)에 과하게 집중하는 것을 막기 위해 사용

```
원본 플레이타임:  1, 2, 3, 5, 10, 50, 10000
                                        ↑ 이 값 때문에 분포가 심하게 치우침

로그 변환 후:    0, 0.7, 1.1, 1.6, 2.3, 3.9, 9.2 → 간격이 훨씬 균등해짐

원본:   ■■■■■■■■■■■■■■■■■■■■                    ← 왼쪽에 몰림
로그:   ■■■■   ■■■■   ■■■■   ■■■■   ■■■■       ← 고르게 펼쳐짐
```

### log1p vs log

> **항상 log1p 사용!**    -> 0이 있는 데이터에서 log는 오류 발생(0에 로그를 할 수 없음)

```python
np.log(0)    # → -inf (오류💥)
np.log1p(0)  # → 0    (정상✅)

# log1p = log(1 + x)
# x가 0이어도 log(1+0) = log(1) = 0
# 플레이타임/투표 수가 0인 유저가 많아서 log1p 사용
```

### 로그 변환 후에도 0에 몰리는 컬럼이 있는 이유

```
votes_funny, comment_count → 대부분 리뷰는 반응이 0개 (구조적 특성)
playtime_last_two_weeks    → 최근 2주 플레이 0 = 이탈한 유저 (59%)
```

**로그 변환 실패가 아니라 데이터 자체의 본질**
→ 0이 많은 컬럼은 이진 플래그(0/1)로 표현하는 게 더 효과적
→ `no_recent_play`, `quit_immediately` 같은 파생 변수를 만든 이유

---

## ⭐ 스케일링이란?

> **컬럼마다 다른 범위를 통일시키는 것**
> 범위가 큰 변수가 모델을 지배하는 것을 막기 위해 사용

```
playtime_forever : 0 ~ 500,000 (범위 매우 큼)
votes_up         : 0 ~ 50      (범위 매우 작음)

→ 스케일링 없으면 모델이 playtime_forever만 보고 판단할 위험
```

### MinMaxScaler — 0과 1 사이로 압축
```
원본:  0    100    500    1000
변환:  0    0.1    0.5    1.0
```

### StandardScaler — 평균 0, 표준편차 1로 변환 (이 프로젝트에서 사용)
```
원본:  100    200    300    400    500
변환: -1.5   -0.75    0    0.75   1.5
              ↑ 평균(300) 기준으로 얼마나 떨어져 있는지
```

### fit_transform vs transform

> **train엔 fit_transform / test엔 transform만!**

```python
# train: 평균/표준편차 계산(fit) + 변환(transform) 동시에
X_train_scaled[num_cols] = scaler.fit_transform(X_train[num_cols])

# test: 이미 계산된 평균/표준편차로 변환만
X_test_scaled[num_cols] = scaler.transform(X_test[num_cols])
```

**test에 fit_transform 쓰면 안 되는 이유:**  
test 기준으로 평균/표준편차를 새로 계산 → train과 다른 기준 적용 → **데이터 누수 발생**

---

## ⭐ 로그 변환 vs 스케일링 차이

| | 목적 | 언제 쓰나 | 순서 |
|---|---|---|---|
| **로그 변환** | 분포의 모양을 바꿈 (치우침 제거) | 극단값이 많은 컬럼 | 먼저 |
| **스케일링** | 범위만 통일 (모양은 유지) | 컬럼 간 단위/범위가 다를 때 | 나중 |

> **순서 중요: 로그 변환 먼저 → 스케일링 나중**
> 분포 모양을 먼저 고친 다음 범위를 통일하는 것

---

## ⭐ 상관계수

> **숫자끼리만 계산 가능**
> `select_dtypes(include=['int64', 'float64', 'bool'])` 로 숫자형 컬럼만 먼저 골라냄

**상관계수 해석 (절댓값 기준)**

| 절댓값 | 관계 강도 | 피처 활용 |
|--------|----------|----------|
| 0.7 이상 | 강한 관계 | ✅ 적극 활용 |
| 0.3 ~ 0.7 | 중간 관계 | ✅ 활용 가능 |
| 0.3 미만 | 약한 관계 | ❌ 제외 고려 |

> **부호(+/-)는 방향, 크기(절댓값)가 강도**
> `-0.61`이면 "강한 음의 관계" → 이 값이 올라가면 churn은 내려감

---

## predict_proba()[:, -1]

모델은 0/1을 바로 뱉지 않고 **확률을 먼저 계산**

```python
rf_model.predict_proba(X_test_scaled)
# 결과:
# [[0.8, 0.2],   ← 유지 80%, 이탈 20%
#  [0.3, 0.7],   ← 유지 30%, 이탈 70%
#  [0.6, 0.4]]   ← 유지 60%, 이탈 40%

[:, -1]  # 마지막 열(이탈 확률)만 뽑기
# → [0.2, 0.7, 0.4]
```

---

## ⭐ Precision vs Recall

> **이탈 예측에서는 Recall이 더 중요**
> 이탈자를 놓치는 비용 > 정상 유저를 이탈로 잘못 분류하는 비용

| | 의미 | 기준 | 질문 |
|-|------|------|------|
| **Precision** | 이탈 예측한 것 중 실제 이탈 비율 | 모델 기준 | 이탈이라고 한 사람 중 몇 명이 진짜야? |
| **Recall** | 실제 이탈자 중 모델이 잡아낸 비율 | 실제 기준 | 실제 이탈 고객 중 몇 명 찾았어? |

**트레이드오프:**
```
Precision ↑ → Recall ↓
Recall ↑    → Precision ↓
한쪽을 올리면 다른 쪽이 희생되는 경향
```

---

## threshold — 이탈 판단 기준선

> **확률을 0/1로 변환하는 기준값**
> 기본값 0.5, 낮출수록 Recall↑ Precision↓

```
threshold = 0.5
- 이탈 확률 0.8 → 이탈(1) ✅
- 이탈 확률 0.3 → 유지(0)
- 이탈 확률 0.45 → 유지(0)

threshold = 0.3 으로 낮추면
- 이탈 확률 0.3 → 이탈(1) ← 0.5일 때는 유지였는데 바뀜
- 이탈 확률 0.45 → 이탈(1) ← 0.5일 때는 유지였는데 바뀜
```

**최적 threshold 찾는 법: Precision-Recall 트레이드오프 확인**
```
threshold   Precision   Recall    의미
  0.3          0.65      0.91    이탈자 많이 잡지만 오탐도 많음
  0.4          0.72      0.87
  0.5          0.79      0.81    ← 이 프로젝트에서 사용한 값
  0.6          0.84      0.73
  0.7          0.89      0.61    정확하지만 이탈자 많이 놓침
```

---

## classification report 해석

```
              precision  recall  f1-score  support
유지(0)          0.71    0.80      0.75     83671
이탈(1)          0.85    0.78      0.81    122461
accuracy                           0.79    206132
```

| 열 | 의미 |
|----|------|
| precision | 이탈이라고 예측한 것 중 실제 이탈 비율 |
| recall | 실제 이탈자 중 모델이 잡아낸 비율 |
| f1-score | precision과 recall의 균형 점수 |
| support | 실제 데이터 수 |
| accuracy | 전체 중 맞춘 비율 |

> **이 프로젝트 기준: 이탈(1) Recall 0.78 → 이탈자 22% 놓침**
> Recall 우선 목표였는데 아쉬운 수치 → threshold 조정, SMOTE, 하이퍼파라미터 튜닝으로 개선 시도

---

## n_estimators / max_depth

**n_estimators = 200** (트리 몇 개 만들지)
- 적으면 (10~50) → 불안정, 실행할 때마다 결과 달라짐
- 많으면 (500+) → 안정적이지만 학습 오래 걸림
- **200 → 속도와 안정성의 균형점 (ML 관행값)**

**max_depth = 12** (트리 하나가 얼마나 깊게 질문할지)
- 얕으면 (3~5) → 너무 단순, 패턴 못 잡음 (과소적합)
- 깊으면 (20+) → 훈련 데이터만 외움 (과적합)
- **12 → 복잡한 패턴은 잡되 과적합은 방지하는 중간값**

> 두 값 모두 경험적 관행값. 정확한 최적값은 GridSearchCV로 찾음
